#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <regex>

using namespace std;

void printIter(vector<string> iter);

void printIter(vector<smatch> iter);

void printIter(map<int, int*> iter);